spring:
  batch:
    job:
      enabled: false
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver

law:
  base-url: https://sgg.gouv.bj/doc
  start-year: 2020
  max-number-per-year: 10
  
  batch:
    chunk-size: 5
    max-threads: 2
    throttle-limit: 1
